// ── Worker URL configuration ─────────────────────────────────
//
// After deploying the worker, paste its URL here. Example:
//
//   export const WORKER_URL = 'https://basira-scraper-worker.your-name.workers.dev';
//
// You can also use a custom domain you've routed to the worker:
//
//   export const WORKER_URL = 'https://api.basira.example';
//
// Don't include a trailing slash.
//
// In development you can keep this pointing at the deployed worker
// even when running Pages locally, since CORS is wide-open by default.

export const WORKER_URL = "https://basira-scraper-worker.YOUR-CF-SUBDOMAIN.workers.dev";
