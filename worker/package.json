{
  "name": "basira-scraper-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for Basira Scraper — runs Playwright on Cloudflare Browser Rendering",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create BASIRA_KV"
  },
  "dependencies": {
    "@cloudflare/playwright": "^1.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
